# brioche
runs on bun only

## in development
options


```
import brioche from '@john-ko/brioche'

brioche({
  // OPTIONS
})

// Default Options
{
  prepend: '/api',
  port: 3000,
  glob: '**/*.route.ts',
  glob_path: './',
}
```
